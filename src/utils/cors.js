const corsApiUrl = 'https://cors-anywhere.herokuapp.com/';

const config = {
  headers: {
    "X-Requested-With": "XMLHttpRequest"
  }
};

// axios.get(`https://cors-anywhere.herokuapp.com/https://www.goodreads.com/search.xml?key=${}&q=${}&page=${}${config}`)
